.profile.container.content
  .row
    = render '/user/shared/user_sidebar'

    .col-md-9
      .profile-body
        = render 'basic_info', product: @product
        = render 'plans', plans: @product.plans
